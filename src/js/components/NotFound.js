

const NotFound = () => (
  <div className="not-found">
    <p>This page cannot be found.<br />
     Please try navigating via the top menu or go <a href="/">to the home page</a>.</p>
  </div>
);

export default NotFound;
